import template from './template'
import styles from './styles'

const appSearch = () => {

    const state = { title: 'AppSearch' }

    return {
        state,
        template,
        styles
    }
}

export default appSearch