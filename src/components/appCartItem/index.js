import template from './template'
import styles from './styles'

const appCartItem = () => {

    const state = {}

    return {
        state,
        template,
        styles
    }
}

export default appCartItem