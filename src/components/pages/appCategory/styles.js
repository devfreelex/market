export default ({ ctx, css }) => css`
    ${ctx} {
        display:flex;
        width:100%;
    }

    ${ctx} .page__category {
        display:flex;
        width:100%;
        background: blue
    }
`